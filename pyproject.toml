[project]
name = "kois_tweaks"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Reuben Daniel", email = "reubendaniel41@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "beet>=0.116.0",
    "bolt>=0.50.1",
]

[project.scripts]
kois_tweaks = "kois_tweaks:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
