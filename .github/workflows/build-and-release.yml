name: Release Beet Project

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v6

    - name: Set up Python 3.14
      uses: actions/setup-python@v6
      with:
        python-version: "3.14"


    - name: Build Beet project
      id: beet
      uses: Trioplane/action-build-beet-project@v1

    - name: Zip build output
      run: |
        echo '${{ steps.beet.outputs.data-packs }}' | jq -r '.[]' | while read dir; do
          name=$(basename "$dir")
          (cd "$dir" && zip -r "${GITHUB_WORKSPACE}/${name}.zip" .)
        done
          
        echo '${{ steps.beet.outputs.resource-packs }}' | jq -r '.[]' | while read dir; do
          name=$(basename "$dir")
          (cd "$dir" && zip -r "${GITHUB_WORKSPACE}/${name}.zip" .)
        done
      
    
    - name: Weld merge packs
      run: |
        python -m venv .weld-env
        source .weld-env/bin/activate

        pip install --upgrade pip
        pip install smithed

        weld $GITHUB_WORKSPACE/*_pack.zip
          
    - name: Create GitHub Release
      uses: softprops/action-gh-release@v2
      with:
        files: "*.zip"
